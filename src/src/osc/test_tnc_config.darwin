# Example TNC config file
# Used by the test_dynamic test suite, but only
# on Mac OS X platforms, where the names of loadable libraries
# is different to Linux and Unix and Cygwin
# It includes files for testing both IMC and IMV
#  $Id: $

IMC "OSC IMC" .libs/libosc_imc.dylib
IMV "OSC IMV" .libs/libosc_imv.dylib
